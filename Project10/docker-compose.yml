version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - playground_data:/app/data
    environment:
      - PYTHONUNBUFFERED=1

volumes:
  playground_data:
