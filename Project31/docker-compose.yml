services:
  app:
    build: .
    image: factory-access-system
    container_name: factory-access-system
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DB_PATH=/data/access.db
    volumes:
      - access_data:/data
    restart: unless-stopped

volumes:
  access_data:
