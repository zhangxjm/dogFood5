version: '3.8'

services:
  web:
    build: .
    container_name: poetry-platform
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - sqlite_data:/app/db.sqlite3
    restart: unless-stopped

volumes:
  sqlite_data:
